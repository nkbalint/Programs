/*#include <stdio.h>
int get_positive_int()
{
    int szam;
    while(1)
    {
        scanf("%d",&szam);

        if (szam>0)
        {
            break;
        }
        else
        {
        printf("Adj meg egy uj szamot: ");

        }
    }
    return szam;
}

int kerulet(int a)
{

return 4*a;
}
int terulet(int a)
{
    return a*a;
}
int main()
{
    printf("Add meg a negyzet oldalhosszat:[egesz] (cm)!");
    int oldal=get_positive_int();
    int k=kerulet(oldal);
    int t=terulet(oldal);

    printf("kerulet: %d cm\n",k);
    printf("terulet:%d cm2\n",t);


    return 0;
}
*/
